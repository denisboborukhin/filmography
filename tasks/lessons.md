# Lessons

- Use the exact commit prefix requested by the user. For this repository it is `feat`, not `fest`; verify the prefix before every commit.
- Do not anchor Filmography language to a specific Markdown app unless the user explicitly asks for that product. Describe inputs as generic Markdown notes.
